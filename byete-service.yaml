apiVersion: v1
kind: Service
metadata:
  name: byete-service
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30081
  selector:
    app: first-depl
  type: NodePort
